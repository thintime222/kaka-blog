export const blogAccentDefault = '#2563eb'

# 全站主题色（配置）

在仓库里改 **`blogAccentDefault`**（`#` + 六位十六进制，如 `#2563eb`），保存后重新构建即可作为**默认强调色**。

在站点里用头像旁调色盘选过的颜色会写在浏览器 `localStorage`（键名见 `page-convention` 里的 `BLOG_ACCENT_STORAGE_KEY`），会**优先于**这里的默认值。
